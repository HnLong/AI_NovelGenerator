<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="NovelEditor.UserControls.NovelControl.NovelItemControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NovelEditor.UserControls.NovelControl"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Margin="12" Padding="16" Background="{ThemeResource SystemControlPageBackgroundChromeLowBrush}" CornerRadius="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" VerticalAlignment="Center">
            <TextBlock Text="{x:Bind NovelTitle, Mode=OneWay}" FontWeight="Bold" FontSize="18" Margin="0,0,0,4"/>
            <TextBlock FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}">
                <Run Text="创建于: "/>
                <Run Text="{x:Bind CreatedDate, Mode=OneWay}"/>
            </TextBlock>
            <TextBlock FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}">
                <Run Text="最后更新: "/>
                <Run Text="{x:Bind UpdatedDate, Mode=OneWay}"/>
            </TextBlock>
        </StackPanel>

        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
            <Button Content="管理" Margin="0,0,8,0" Style="{ThemeResource AccentButtonStyle}"/>
            <Button Content="删除" x:Name="DeleteButton" Click="DeleteButton_Click"/>
        </StackPanel>
    </Grid>
</UserControl>

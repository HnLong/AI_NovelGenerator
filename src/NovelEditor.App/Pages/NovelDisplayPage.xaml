<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="NovelEditor.Pages.NovelDisplayPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NovelEditor.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <ScrollViewer>
            <StackPanel Margin="24">
                <TextBlock Text="我的小说" FontSize="28" FontWeight="Bold" Margin="0,0,0,24"/>

                <!-- 小说列表 -->
                <ListView x:Name="NovelsListView" SelectionMode="None" CanReorderItems="False" CanDragItems="False">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0,0,0,8"/>
                        </Style>
                    </ListView.ItemContainerStyle>
                </ListView>

                <!-- 没有小说时的提示信息 -->
                <StackPanel x:Name="NoNovelsPanel" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,100,0,0">
                    <TextBlock Text="当前暂无小说" FontSize="20" HorizontalAlignment="Center" Margin="0,0,0,12"/>
                    <Button x:Name="CreateNovelButton" Content="点击创建小说" Style="{ThemeResource AccentButtonStyle}" Click="CreateNovelButton_Click"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
